---
name: smart-commit
description: Stub — redireciona para a skill canônica git-commit. Use quando o usuário pedir para fazer commit, commitar mudanças ou gerar mensagem de commit.
---

# Smart Commit Helper (stub)

Esta skill foi unificada com `.cursor/skills/git-commit/`.

Use **git-commit** para analisar o diff, executar revisões quando necessário e gerar mensagens de commit seguindo as convenções do projeto ([docs/CONVENTIONS.md](../../../docs/CONVENTIONS.md)).
