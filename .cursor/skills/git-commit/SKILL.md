---
name: git-commit
description: Stub — redireciona para a skill canônica smart-commit. Use quando o usuário pedir para fazer commit, commitar mudanças, ou gerar mensagem de commit.
---

# Git Commit Helper (stub)

Esta skill foi unificada com `.cursor/skills/smart-commit/`.

Use **smart-commit** para analisar o diff e gerar mensagens de commit seguindo as convenções do projeto ([docs/CONVENTIONS.md](../../../docs/CONVENTIONS.md)).
