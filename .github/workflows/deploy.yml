name: Deploy Production

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4

      - name: Copy production env
        run: cp .env.prod.example .env

      - name: Build production images
        run: docker-compose -f docker-compose.prod.yml build

      # Descomente e configure após definir secrets e registry de deploy:
      # - name: Login to container registry
      #   uses: docker/login-action@v3
      #   with:
      #     registry: ${{ secrets.REGISTRY_URL }}
      #     username: ${{ secrets.REGISTRY_USERNAME }}
      #     password: ${{ secrets.REGISTRY_PASSWORD }}
      #
      # - name: Push images
      #   run: docker-compose -f docker-compose.prod.yml push
      #
      # - name: Deploy to remote host
      #   run: |
      #     # Comando SSH/RSYNC para deploy no servidor de produção
      #     # ssh ${{ secrets.DEPLOY_USER }}@${{ secrets.DEPLOY_HOST }} "cd /app && docker-compose -f docker-compose.prod.yml pull && docker-compose -f docker-compose.prod.yml up -d"

      - name: Deploy summary
        run: echo "Build de produção concluído. Push/deploy remoto requer registry e secrets configurados."
